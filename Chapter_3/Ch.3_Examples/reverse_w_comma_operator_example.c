#include <stdio.h>
#include <stdlib.h>

char * expand(char s1[], char s2[]);

int main(int argc, char *argv[]){
	printf("%s\n", reverse(argv[1]));
	return 0;
}

char * expand(char s1[], char s2[]){
	
	int i;
	if(i = 0; s1[i] != '\0'; i++){
		if(isdigit(s1[i]) || ischar(s1[i]))
	}
	return s1;
}
